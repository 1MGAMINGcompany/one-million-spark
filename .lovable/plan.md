# Finalize Custom-Event Pool Model (Treasury Custody)\n\nAudit confirmed the core flow is wired correctly. These four fixes close the remaining gaps so the pool model works end-to-end without orphaned funds or misrouted payouts. **Polymarket events are not touched.**\n\n---\n\n## 1. `prediction-auto-claim` → pay from Treasury\n\nCurrently the cron worker still pays winners using `FEE_RELAYER_PRIVATE_KEY`. After this change, custom-event payouts will use `TREASURY_PRIVATE_KEY` (matching `prediction-claim`).\n\n- Update `transferUsdcFromTreasury()` helper to read `TREASURY_PRIVATE_KEY` first\n- Add legacy fallback: if a `prediction_entries` row has `stake_tx_hash IS NULL`, the stake never reached Treasury → fall back to `FEE_RELAYER_PRIVATE_KEY` (old flow) so existing in-flight entries still pay out\n- Polymarket branch (CTF redemption from derived EOA) — **unchanged**\n\n## 2. Persist `stake_tx_hash` in `prediction-submit`\n\nThe migrat